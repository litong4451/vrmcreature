name: Build & Release

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Build mod
        run: |
          chmod +x gradlew
          ./gradlew build --no-daemon

      - name: Prepare release files
        run: |
          mkdir -p release
          find build/libs -maxdepth 1 -name '*.jar' \
            ! -name '*sources*' ! -name '*javadoc*' ! -name '*-api*' ! -name '*-dev*' \
            -exec cp {} release/ \;
          # tag 触发时按 tag 版本重命名 jar（去掉 v 前缀），如 v1.0.2 -> vrmcreature-1.0.2.jar
          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            VER="${GITHUB_REF_NAME#v}"
            for f in release/*.jar; do
              mv "$f" "release/vrmcreature-${VER}.jar"
            done
          fi

      - name: Upload jar (artifact)
        uses: actions/upload-artifact@v4
        with:
          name: vrmcreature-jar
          path: release/*.jar

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: release/*.jar
          name: vrmcreature ${{ github.ref_name }}
          generate_release_notes: true
